# "authorized" is the only identity with any granted topic -- everyone else,
# including "stranger" below, gets nothing (mosquitto denies by default once an
# acl_file is set at all). smoke.sh checks both sides of that.
user authorized
topic readwrite mes/smoke/#

user stranger
